pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Code Quality') {
            steps {
                echo 'ESLint Stage'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t devops-assignment .'
            }
        }
    }
}
