pipeline {
    agent any

    stages {

        stage('Code Quality - ESLint') {
            steps {
                sh 'npm install'
                sh 'npx eslint . || true'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t devops-assignment .'
            }
        }

    }
}
