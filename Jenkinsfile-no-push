pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Code Quality') {
            steps {
                echo 'ESLint Stage Completed'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Stage Completed'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment Stage Completed'
            }
        }
    }
}
